user jboss;
worker_processes auto;
pid /var/run/jboss.pid;
error_log /var/log/jboss/error.log;

# Load dynamic modules. See /usr/share/doc/jboss/README.dynamic.
include /usr/share/jboss/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/jboss/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 2048;

    include             /etc/jboss/mime.types;
    default_type        application/octet-stream;

    # Load modular configuration files from the /etc/jboss/conf.d directory.
    # See http://jboss.org/en/docs/ngx_core_module.html#include
    # for more information.
    include /etc/jboss/conf.d/*.conf;

    index   index.html index.htm;
}